//============================================================================================================
// C O P Y R I G H T
//------------------------------------------------------------------------------------------------------------
/// \copyright (C) 2021 Robert Bosch GmbH.
//
// The reproduction, distribution and utilization of this file as
// well as the communication of its contents to others without express
// authorization is prohibited. Offenders will be held liable for the
// payment of damages. All rights reserved in the event of the grant
// of a patent, utility model or design.
//============================================================================================================

#ifndef DATA_HANDLING_H
#define DATA_HANDLING_H

/// standard includes
#include <fstream>
#include <iostream>
#include <string>
#include <vector>

#include <stdint.h>
#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include <map>
#include "../../Includes/cdll.h"
#include "../../Includes/VIA.h"
#include "../../Includes/VIA_CDLL.h"

/**
 * @brief TODO
*/

void copyBitsToByteArrayBE(uint64_t valueTocopy, byte byteArray[], int64 startBitIndex, int numBits)//Motorola
{

    int i;
    int byteIndex;
    int bitIndex;
    byte bitValue;
  
    for (i = 0; i < numBits; i++)
    {
        byteIndex = (int)startBitIndex / 8;
        bitIndex = (int)startBitIndex % 8;
  
        bitValue = (byte) (valueTocopy >> i) & 1;

        byteArray[byteIndex] |= (byte)(bitValue << bitIndex);
  
        if (bitIndex == 7)
        {
            startBitIndex -= 15;
        }
        else
        {
            startBitIndex++;
        }
    }
}

void copyBitsToByteArrayLE(uint64_t valueToCopy, byte byteArray[], int64 startBitIndex, int numBits)//Intel
{
  int i;
  int byteIndex;
  int bitIndex;
  int byteCnt;

  byteIndex = (int)startBitIndex / 8;
  bitIndex = (int)startBitIndex % 8;
  byteCnt = (bitIndex + numBits + 7) / 8;

  valueToCopy &= ((1<<numBits)-1); // limit value to numBits

  for (i = 0; i < byteCnt; ++i)
  {
    if(i==0)
      byteArray[byteIndex + i] |= (byte)(valueToCopy << bitIndex);
    else
      byteArray[byteIndex + i] |= (byte)(valueToCopy >> (i*8 - bitIndex));
  }
}



uint64_t convertPhysicalToRaw(double physicalValue, double Factor, double offset)
{
    return (uint64_t)((physicalValue - offset) / Factor);
}

uint64_t copyBitsFromByteArrayLE( byte byteArray[], int64 startBitIndex, int64 numBits)
{
  uint64_t result;
  byte temp;
  int64 byteIndex,bitIndex;
  int64 endBitIndex;
  int64 i;//For looping

  result = 0;
  endBitIndex = startBitIndex + numBits - 1;
  for (i = startBitIndex; i <= endBitIndex; i++) 
  {
    byteIndex = i / 8;
    bitIndex = i % 8;
    temp = (byteArray[byteIndex] >> bitIndex) & 0x01;
    result |= temp << (i - startBitIndex);
  }
  return result;
}

uint64_t copyBitsFromByteArrayBE(byte byteArray[], int64 startBitIndex, int64 numBits)
{
  uint64_t result;
  byte temp;
  int64 byteIndex, bitIndex;
  int64 endBitIndex;
  int64 i; // For looping

  result = 0;
  endBitIndex = startBitIndex + numBits - 1;
  for (i = endBitIndex; i >= startBitIndex; i--)
  {
    byteIndex = i / 8;
    bitIndex = 7 - (i % 8); // Reverse the bit order for big-endian
    temp = (byteArray[byteIndex] >> bitIndex) & 0x01;
    result |= temp << (endBitIndex - i);
  }
  return result;
}


void RXX_Location_Loc500_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 96864, 11);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 96960, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 96832, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 96848, 15);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 96875, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 96885, 10);

    raw_value = 30; // Mistake this is actually the RadialVelocityVnce and should be 30
    copyBitsToByteArrayLE(raw_value, byteArray, 96896, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 96905, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 
write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 93803, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 93824, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 93813, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 93840, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 93792, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 93776, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 93760, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 93833, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 93877, 9);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 93856, 11);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 93867, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 93888, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 93895, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 93902, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 93909, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 93920, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 93928, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 93936, 7);
}


void RXX_Location_Loc483_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 93611, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 93641, 7);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 93632, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 93621, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 93648, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 93600, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 93584, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 93568, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 93664, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 93685, 9);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 93675, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 93696, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 93703, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 93710, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 93717, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 93728, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 93736, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 93744, 7);
}


void RXX_Location_Loc482_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 93472, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 93376, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 93392, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 93408, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 93419, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 93429, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 93440, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 93449, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 93456, 14);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 93544, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 93493, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 93504, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 93511, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 93518, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 93525, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 93536, 8);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 93483, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 93552, 7);
}


void RXX_Location_Loc481_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 93227, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 93248, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 93237, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 93264, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 93216, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 93200, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 93184, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 93257, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 93301, 9);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 93280, 11);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 93326, 7);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 93291, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 93344, 8);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 93333, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 93352, 8);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 93319, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 93312, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 93360, 7);
}


void RXX_Location_Loc480_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 93072, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 92992, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 93008, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 93024, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 93035, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 93045, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 93056, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 93065, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 93109, 9);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 93088, 11);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 93099, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 93120, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 93127, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 93134, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 93141, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 93152, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 93160, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 93168, 7);
}


void RXX_Location_Loc479_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 92843, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 92864, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 92853, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 92880, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 92832, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 92816, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 92800, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 92873, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 92968, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 92896, 11);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 92942, 7);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 92907, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 92960, 8);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 92949, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 92976, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 92935, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 92928, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 92917, 9);
}


void RXX_Location_Loc478_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 92688, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 92608, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 92624, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 92640, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 92651, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 92661, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 92672, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 92681, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 92704, 11);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 92776, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 92725, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 92736, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 92743, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 92750, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 92757, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 92768, 8);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 92715, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 92784, 7);
}


void RXX_Location_Loc477_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 92459, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 92480, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 92469, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 92496, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 92448, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 92432, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 92416, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 92489, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 92512, 11);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 92584, 8);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 92558, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 92592, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 92576, 8);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 92565, 10);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 92523, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 92551, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 92544, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 92533, 9);
}


void RXX_Location_Loc476_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 92304, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 92224, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 92240, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 92256, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 92267, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 92277, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 92288, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 92297, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 92320, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 92341, 9);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 92331, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 92352, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 92359, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 92366, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 92373, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 92384, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 92392, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 92400, 7);
}


void RXX_Location_Loc475_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 92075, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 92096, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 92085, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 92112, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 92064, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 92048, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 92032, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 92105, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 92128, 11);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 92139, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 92174, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 92208, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 92192, 8);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 92181, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 92200, 8);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 92167, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 92160, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 92149, 9);
}


void RXX_Location_Loc474_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 91936, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 91840, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 91856, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 91872, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 91883, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 91893, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 91904, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 91913, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 91920, 14);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 92016, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 91957, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 91968, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 91975, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 91982, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 91989, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 92000, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 92008, 8);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 91947, 10);
}


void RXX_Location_Loc473_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 91691, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 91721, 7);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 91712, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 91701, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 91728, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 91680, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 91664, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 91648, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 91744, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 91765, 9);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 91755, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 91776, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 91783, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 91790, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 91797, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 91808, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 91824, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 91816, 8);
}


void RXX_Location_Loc472_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 91536, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 91456, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 91472, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 91488, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 91499, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 91509, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 91520, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 91529, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 91573, 9);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 91552, 11);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 91563, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 91584, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 91591, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 91598, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 91605, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 91616, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 91624, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 91632, 7);
}


void RXX_Location_Loc471_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 91307, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 91328, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 91317, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 91344, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 91296, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 91280, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 91264, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 91337, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 91440, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 91360, 11);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 91406, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 91432, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 91424, 8);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 91413, 10);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 91371, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 91399, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 91392, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 91381, 9);
}


void RXX_Location_Loc470_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 91152, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 91072, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 91088, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 91104, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 91115, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 91125, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 91136, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 91145, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 91168, 11);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 91240, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 91189, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 91200, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 91207, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 91214, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 91221, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 91232, 8);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 91179, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 91248, 7);
}


void RXX_Location_Loc469_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 90923, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 90944, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 90933, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 90960, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 90912, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 90896, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 90880, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 90953, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 90976, 11);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 91048, 8);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 91022, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 91056, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 91040, 8);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 91029, 10);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 90987, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 91015, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 91008, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray,
 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 87950, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 87957, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 87968, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 87976, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 87984, 7);
}


void RXX_Location_Loc452_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 87659, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 87680, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 87669, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 87696, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 87648, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 87632, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 87616, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 87689, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 87792, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 87712, 11);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 87758, 7);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 87723, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 87776, 8);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 87765, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 87784, 8);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 87751, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 87744, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 87733, 9);
}


void RXX_Location_Loc451_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 87504, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 87424, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 87440, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 87456, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 87467, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 87477, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 87488, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 87497, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 87520, 11);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 87592, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 87541, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 87552, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 87559, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 87566, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 87573, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 87584, 8);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 87531, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 87600, 7);
}


void RXX_Location_Loc450_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 87275, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 87296, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 87285, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 87312, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 87264, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 87248, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 87232, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 87305, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 87408, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 87328, 11);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 87374, 7);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 87339, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 87392, 8);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 87381, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 87400, 8);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 87367, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 87360, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 87349, 9);
}


void RXX_Location_Loc449_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 87040, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 87056, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 87072, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 87083, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 87093, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 87104, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 87113, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 87120, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 87136, 11);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 87147, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 87189, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 87157, 9);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 87208, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 87200, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 87216, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 87182, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 87175, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 87168, 7);
}


void RXX_Location_Loc448_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 86928, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 86848, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 86864, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 86880, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 86891, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 86901, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 86912, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 86921, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 86944, 11);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 87016, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 86965, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 86976, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 86983, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 86990, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 86997, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 87008, 8);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 86955, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 87024, 7);
}


void RXX_Location_Loc447_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 86699, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 86720, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 86709, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 86736, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 86688, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 86672, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 86656, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 86729, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 86752, 11);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 86824, 8);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 86798, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 86832, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 86816, 8);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 86805, 10);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 86763, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 86791, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 86784, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 86773, 9);
}


void RXX_Location_Loc446_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 86544, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 86464, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 86480, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 86496, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 86507, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 86517, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 86528, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 86537, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 86560, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 86581, 9);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 86571, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 86592, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 86599, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 86606, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 86613, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 86624, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 86632, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 86640, 7);
}


void RXX_Location_Loc445_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 86315, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 86336, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 86325, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 86352, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 86304, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 86288, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 86272, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 86345, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 86368, 11);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 86379, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 86414, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 86448, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 86432, 8);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 86421, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 86440, 8);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 86407, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 86400, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 86389, 9);
}


void RXX_Location_Loc444_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 86176, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 86080, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 86096, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 86112, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 86123, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 86133, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 86144, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 86153, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 86160, 14);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 86256, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 86197, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 86208, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 86215, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 86222, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 86229, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 86240, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 86248, 8);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 86187, 10);
}


void RXX_Location_Loc443_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 85931, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 85961, 7);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 85952, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 85941, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 85968, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 85920, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 85904, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 85888, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 85984, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 86005, 9);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 85995, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 86016, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 86023, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 86030, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 86037, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 86048, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 86064, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 86056, 8);
}


void RXX_Location_Loc442_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 85776, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 85696, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 85712, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 85728, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 85739, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 85749, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 85760, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 85769, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 85813, 9);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 85792, 11);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 85803, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 85824, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 85831, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 85838, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 85845, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 85856, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 85864, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 85872, 7);
}


void RXX_Location_Loc441_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 85547, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 85568, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 85557, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 85584, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 85536, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 85520, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 85504, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 85577, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 85680, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 85600, 11);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 85646, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 85672, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 85664, 8);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 85653, 10);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 85611, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 85639, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 85632, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 85621, 9);
}


void RXX_Location_Loc440_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 85392, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 85312, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 85328, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 85344, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 85355, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 85365, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 85376, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 85385, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 85408, 11);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 85480, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 85429, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 85440, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 85447, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 85454, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 85461, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 85472, 8);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 85419, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 85488, 7);
}


void RXX_Location_Loc439_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 85163, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 85184, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 85173, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 85200, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 85152, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 85136, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 85120, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 85193, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 85216, 11);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 85288, 8);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 85262, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 85296, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 85280, 8);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 85269, 10);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 85227, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 85255, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 85248, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 85237, 9);
}


void RXX_Location_Loc438_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 85008, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 84928, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 84944, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 84960, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 84971, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 84981, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 84992, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 85001, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 85024, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 85045, 9);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 85035, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 85056, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 85063, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 85070, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 85077, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 85088, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 85096, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 85104, 7);
}


void RXX_Location_Loc437_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 84779, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 84800, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 84789, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 84816, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 84768, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 84752, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 84736, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 84809, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 84912, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 84832, 11);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 
 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 48320, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 48329, 7);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 48309, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 48352, 11);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 48391, 7);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 48363, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 48405, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 48398, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 48416, 8);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 48384, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 48373, 9);
}


void RXX_Location_Loc246_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 48160, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 48064, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 48080, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 48096, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 48107, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 48117, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 48128, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 48137, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 48144, 14);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 48206, 7);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 48171, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 48181, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 48192, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 48199, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 48213, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 48224, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 48232, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 48240, 7);
}


void RXX_Location_Loc245_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 47968, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 47888, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 47904, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 47915, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 47925, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 47936, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 47945, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 47952, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 47872, 16);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 47979, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 48021, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 47989, 9);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 48040, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 48032, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 48048, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 48014, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 48007, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 48000, 7);
}


void RXX_Location_Loc244_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 47760, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 47680, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 47696, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 47712, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 47723, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 47733, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 47744, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 47753, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 47776, 11);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 47848, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 47797, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 47808, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 47815, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 47822, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 47829, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 47840, 8);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 47787, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 47856, 7);
}


void RXX_Location_Loc243_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 47531, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 47552, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 47541, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 47568, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 47520, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 47504, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 47488, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 47561, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 47584, 11);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 47656, 8);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 47630, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 47664, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 47648, 8);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 47637, 10);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 47595, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 47623, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 47616, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 47605, 9);
}


void RXX_Location_Loc242_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 47376, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 47296, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 47312, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 47328, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 47339, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 47349, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 47360, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 47369, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 47392, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 47413, 9);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 47403, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 47424, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 47431, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 47438, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 47445, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 47456, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 47464, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 47472, 7);
}


void RXX_Location_Loc241_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 47147, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 47168, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 47157, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 47184, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 47136, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 47120, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 47104, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 47177, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 47200, 11);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 47211, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 47246, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 47280, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 47264, 8);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 47253, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 47272, 8);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 47239, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 47232, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 47221, 9);
}


void RXX_Location_Loc240_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 47008, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 46912, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 46928, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 46944, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 46955, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 46965, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 46976, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 46985, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 46992, 14);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 47088, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 47029, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 47040, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 47047, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 47054, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 47061, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 47072, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 47080, 8);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 47019, 10);
}


void RXX_Location_Loc239_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 46763, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 46793, 7);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 46784, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 46773, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 46800, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 46752, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 46736, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 46720, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 46816, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 46837, 9);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 46827, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 46848, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 46855, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 46862, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 46869, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 46880, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 46896, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 46888, 8);
}


void RXX_Location_Loc238_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 46608, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 46528, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 46544, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 46560, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 46571, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 46581, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 46592, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 46601, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 46645, 9);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 46624, 11);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 46635, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 46656, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 46663, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 46670, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 46677, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 46688, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 46696, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 46704, 7);
}


void RXX_Location_Loc237_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 46379, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 46400, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 46389, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 46416, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 46368, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 46352, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 46336, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 46409, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 46432, 11);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 46443, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 46478, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 46504, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 46496, 8);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 46485, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 46512, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 46471, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 46464, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 46453, 9);
}


void RXX_Location_Loc236_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 46224, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 46144, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 46160, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 46176, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 46187, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 46197, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 46208, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 46217, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 46240, 11);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 46312, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 46261, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 46272, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 46279, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 46286, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 46293, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 46304, 8);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 46251, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 46320, 7);
}


void RXX_Location_Loc235_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 45995, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 46016, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 46005, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 46048, 11);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 45984, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 45968, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 45952, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 46025, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 46032, 14);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 46128, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 46069, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 46080, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 46087, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 46094, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 46101, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 46112, 8);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 46059, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 46120, 8);
}


void RXX_Location_Loc234_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 45803, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 45824, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 45813, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 45840, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 45792, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 45776, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 45760, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 45833, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 45877, 9);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 45856, 11);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 45867, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 45888, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 45895, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 45902, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 45909, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 45920, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 45928, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 45936, 7);
}


void RXX_Location_Loc233_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 45611, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 45641, 7);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 45632, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 45621, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 45648, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 45600, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 45584, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 45568, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 45664, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 45685, 9);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 45675, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 45696, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 45703, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 45710, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 45717, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 45728, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 45736, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 45744, 7);
}


void RXX_Location_Loc232_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 45472, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 45376, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 45392, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 45408, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 45419, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 45429, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 45440, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 45449, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 45456, 14);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 45544, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 45493, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 45504, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 45511, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 45518, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 45525, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 45536, 8);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 45483, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 45552, 7);
}


void RXX_Location_Loc231_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 45227, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 45248, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray,
 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 36328, 8);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 36295, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 36288, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 36277, 9);
}


void RXX_Location_Loc183_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 36048, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 35968, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 35984, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 36000, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 36011, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 36021, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 36032, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 36041, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 36064, 11);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 36136, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 36085, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 36096, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 36103, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 36110, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 36117, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 36128, 8);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 36075, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 36144, 7);
}


void RXX_Location_Loc182_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 35819, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 35840, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 35829, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 35856, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 35808, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 35792, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 35776, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 35849, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 35872, 11);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 35944, 8);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 35918, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 35952, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 35936, 8);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 35925, 10);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 35883, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 35911, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 35904, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 35893, 9);
}


void RXX_Location_Loc181_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 35664, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 35584, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 35600, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 35616, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 35627, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 35637, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 35648, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 35657, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 35680, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 35701, 9);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 35691, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 35712, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 35719, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 35726, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 35733, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 35744, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 35752, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 35760, 7);
}


void RXX_Location_Loc180_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 35435, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 35456, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 35445, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 35472, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 35424, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 35408, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 35392, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 35465, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 35488, 11);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 35499, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 35534, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 35568, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 35552, 8);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 35541, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 35560, 8);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 35527, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 35520, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 35509, 9);
}


void RXX_Location_Loc179_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 35296, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 35200, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 35216, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 35232, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 35243, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 35253, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 35264, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 35273, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 35280, 14);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 35376, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 35317, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 35328, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 35335, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 35342, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 35349, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 35360, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 35368, 8);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 35307, 10);
}


void RXX_Location_Loc178_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 35051, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 35081, 7);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 35072, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 35061, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 35088, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 35040, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 35024, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 35008, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 35104, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 35125, 9);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 35115, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 35136, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 35143, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 35150, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 35157, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 35168, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 35184, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 35176, 8);
}


void RXX_Location_Loc177_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 34896, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 34816, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 34832, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 34848, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 34859, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 34869, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 34880, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 34889, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 34933, 9);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 34912, 11);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 34923, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 34944, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 34951, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 34958, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 34965, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 34976, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 34984, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 34992, 7);
}


void RXX_Location_Loc176_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 34667, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 34688, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 34677, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 34704, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 34656, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 34640, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 34624, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 34697, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 34800, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 34720, 11);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 34766, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 34792, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 34784, 8);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 34773, 10);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 34731, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 34759, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 34752, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 34741, 9);
}


void RXX_Location_Loc175_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 34512, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 34432, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 34448, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 34464, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 34475, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 34485, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 34496, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 34505, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 34528, 11);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 34600, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 34549, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 34560, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 34567, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 34574, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 34581, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 34592, 8);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 34539, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 34608, 7);
}


void RXX_Location_Loc174_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 34283, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 34304, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 34293, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 34320, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 34272, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 34256, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 34240, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 34313, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 34336, 11);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 34408, 8);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 34382, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 34416, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 34400, 8);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 34389, 10);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 34347, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 34375, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 34368, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 34357, 9);
}


void RXX_Location_Loc173_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 34128, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 34048, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 34064, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 34080, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 34091, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 34101, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 34112, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 34121, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 34144, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 34165, 9);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 34155, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 34176, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 34183, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 34190, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 34197, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 34208, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 34216, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 34224, 7);
}


void RXX_Location_Loc172_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 33899, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 33920, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 33909, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 33936, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 33888, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 33872, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 33856, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 33929, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 34032, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 33952, 11);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 33998, 7);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 33963, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 34016, 8);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 34005, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 34024, 8);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 33991, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 33984, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 33973, 9);
}


void RXX_Location_Loc171_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 33744, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 33664, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 33680, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 33696, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 33707, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 33717, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 33728, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 33737, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 33760, 11);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 33832, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 33781, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 33792, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 33799, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 33806, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 33813, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 33824, 8);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 33771, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 33840, 7);
}


void RXX_Location_Loc170_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 33515, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 33536, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 33525, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 33552, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 33504, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 33488, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 33472, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 33545, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 33648, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 33568, 11);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 33614, 7);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 33579, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 33632, 8);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 33621, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 33640, 8);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 33607, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 33600, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 33589, 9);
}


void RXX_Location_Loc169_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 33280, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 33296, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 33312, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 33323, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 33333, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 33344, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 33353, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 33360, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 33376, 11);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 33387, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 33429, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 33397, 9);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 33448, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 33440, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 33456, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 33422, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 33415, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 33408, 7);
}


void RXX_Location_Loc168_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 33168, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 33088, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 33104, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 33120, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 33131, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 33141, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 33152, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 33161, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 33184, 11);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 33256, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray,
 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 26987, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 26997, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 27008, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 27017, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 27040, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 27061, 9);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 27051, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 27072, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 27079, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 27086, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 27093, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 27104, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 27112, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 27120, 7);
}


void RXX_Location_Loc135_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 26795, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 26816, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 26805, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 26832, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 26784, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 26768, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 26752, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 26825, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 26848, 11);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 26859, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 26894, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 26928, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 26912, 8);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 26901, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 26920, 8);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 26887, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 26880, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 26869, 9);
}


void RXX_Location_Loc134_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 26656, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 26560, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 26576, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 26592, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 26603, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 26613, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 26624, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 26633, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 26640, 14);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 26736, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 26677, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 26688, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 26695, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 26702, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 26709, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 26720, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 26728, 8);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 26667, 10);
}


void RXX_Location_Loc133_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 26411, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 26441, 7);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 26432, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 26421, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 26448, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 26400, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 26384, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 26368, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 26464, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 26485, 9);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 26475, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 26496, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 26503, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 26510, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 26517, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 26528, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 26544, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 26536, 8);
}


void RXX_Location_Loc132_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 26256, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 26176, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 26192, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 26208, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 26219, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 26229, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 26240, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 26249, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 26293, 9);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 26272, 11);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 26283, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 26304, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 26311, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 26318, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 26325, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 26336, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 26344, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 26352, 7);
}


void RXX_Location_Loc131_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 26027, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 26048, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 26037, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 26064, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 26016, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 26000, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 25984, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 26057, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 26160, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 26080, 11);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 26126, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 26152, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 26144, 8);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 26133, 10);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 26091, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 26119, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 26112, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 26101, 9);
}


void RXX_Location_Loc130_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 25872, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 25792, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 25808, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 25824, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 25835, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 25845, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 25856, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 25865, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 25888, 11);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 25960, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 25909, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 25920, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 25927, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 25934, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 25941, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 25952, 8);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 25899, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 25968, 7);
}


void RXX_Location_Loc129_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 25643, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 25664, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 25653, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 25680, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 25632, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 25616, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 25600, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 25673, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 25696, 11);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 25768, 8);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 25742, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 25776, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 25760, 8);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 25749, 10);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 25707, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 25735, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 25728, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 25717, 9);
}


void RXX_Location_Loc128_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 25488, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 25408, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 25424, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 25440, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 25451, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 25461, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 25472, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 25481, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 25504, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 25525, 9);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 25515, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 25536, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 25543, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 25550, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 25557, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 25568, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 25576, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 25584, 7);
}


void RXX_Location_Loc127_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 25259, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 25280, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 25269, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 25296, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 25248, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 25232, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 25216, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 25289, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 25392, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 25312, 11);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 25358, 7);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 25323, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 25376, 8);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 25365, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 25384, 8);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 25351, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 25344, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 25333, 9);
}


void RXX_Location_Loc126_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 25104, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 25024, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 25040, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 25056, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 25067, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 25077, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 25088, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 25097, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 25120, 11);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 25192, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 25141, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 25152, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 25159, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 25166, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 25173, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 25184, 8);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 25131, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 25200, 7);
}


void RXX_Location_Loc125_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 24875, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 24896, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 24885, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 24912, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 24864, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 24848, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 24832, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 24905, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 25008, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 24928, 11);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 24974, 7);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 24939, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 24992, 8);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 24981, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 25000, 8);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 24967, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 24960, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 24949, 9);
}


void RXX_Location_Loc124_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 24640, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 24656, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 24672, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 24683, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 24693, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 24704, 9);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 24736, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 24720, 14);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 24713, 7);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 24747, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 24789, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 24816, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 24808, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 24800, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 24757, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 24782, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 24775, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 24768, 7);
}


void RXX_Location_Loc123_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 24544, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 24448, 16);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 24480, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 24491, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 24501, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 24512, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 24521, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 24528, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 24464, 15);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 24555, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 24597, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 24624, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 24616, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 24608, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 24565, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 24590, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 24583, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 24576, 7);
}


void RXX_Location_Loc122_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 24336, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 24256, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 24272, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 24288, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 24299, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 24309, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 24320, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 24329, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 24352, 11);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 24424, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 24373, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 24384, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 24391, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 24398, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 24405, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 24416, 8);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 24363, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 24432, 7);
}


void RXX_Location_Loc121_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 24107, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 24128, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 24117, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 24144, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 24096, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 24080, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 24064, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 24137, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 24160, 11);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 24232, 8);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 24206, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 24240, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 24224, 8);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 24213, 10);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 24171, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 24199, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 24192, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 24181, 9);
}


void RXX_Location_Loc120_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 23952, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 23872, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 23888, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 23904, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 23915, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 23925, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 23936, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 23945, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 23968, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 23989, 9);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 23979, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 24000, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 24007, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 24014, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 24021, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 24032, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 24040, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 24048, 7);
}


void RXX_Location_Loc119_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 23723, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 23744, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 23733, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 23760, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 23712, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 23696, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 23680, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 23753, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 23776, 11);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 23787, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 23822, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 23856, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 23840, 8);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 23829, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 23848, 8);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 23815, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 23808, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 23797, 9);
}


void RXX_Location_Loc118_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 23584, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 23488, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 23504, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 23520, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 23531, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 23541, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 23552, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 23561, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 23568, 14);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 23664, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 23605, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 23616, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 23623, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 23630, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 23637, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 23648, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 23656, 8);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 23595, 10);
}


void RXX_Location_Loc117_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 23339, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 23369, 7);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 23360, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 23349, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 23376, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 23328, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 23312, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 23296, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 23392, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 23413, 9);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 23403, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 23424, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 23431, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 23438, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 23445, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 23456, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 23472, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 23464, 8);
}


void RXX_Location_Loc116_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 23184, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 23104, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 23120, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 23136, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 23147, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 23157, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 23168, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 23177, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 23221, 9);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 23200, 11);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 23211, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 23232, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 23239, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 23246, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 23253, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 23264, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 23272, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 23280, 7);
}


void RXX_Location_Loc115_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 22955, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 22976, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 22965, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 22992, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 22944, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 22928, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 22912, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 22985, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 23008, 11);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 23019, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 23054, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 23080, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 23072, 8);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 23061, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 23088, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 23047, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 23040, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 23029, 9);
}


void RXX_Location_Loc114_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 22800, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 22720, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 22736, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 22752, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 22763, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 22773, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 22784, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 22793, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 22816, 11);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 22888, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 22837, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 22848, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 22855, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 22862, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 22869, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 22880, 8);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 22827, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 22896, 7);
}


void RXX_Location_Loc113_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 22571, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 22592, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 22581, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 22624, 11);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 22560, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 22544, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 22528, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 22601, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 22608, 14);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 22704, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 22645, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 22656, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 22663, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 22670, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 22677, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 22688, 8);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 22635, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 22696, 8);
}


void RXX_Location_Loc112_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 22379, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 22400, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 22389, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 22416, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 22368, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 22352, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 22336, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 22409, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 22453, 9);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 22432, 11);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 22443, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 22464, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 22471, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 22478, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 22485, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 22496, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 22504, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 22512, 7);
}


void RXX_Location_Loc111_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 22187, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 22217, 7);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 22208, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 22197, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 22224, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 22176, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 22160, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 22144, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 22240, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 22261, 9);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 22251, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 22272, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 22279, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 22286, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 22293, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 22304, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 22312, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 22320, 7);
}


void RXX_Location_Loc110_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 22048, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 21952, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 21968, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 21984, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 21995, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 22005, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 22016, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 22025, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 22032, 14);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 22120, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 22069, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 22080, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 22087, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 22094, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 22101, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 22112, 8);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 22059, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 22128, 7);
}


void RXX_Location_Loc109_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 21803, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 21824, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 21813, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 21840, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 21792, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 21776, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 21760, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 21833, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 21877, 9);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 21856, 11);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 21902, 7);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 21867, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 21920, 8);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 21909, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 21928, 8);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 21895, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 21888, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 21936, 7);
}


void RXX_Location_Loc108_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 21648, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 21568, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 21584, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 21600, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 21611, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 21621, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 21632, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 21641, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 21685, 9);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 21664, 11);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 21675, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 21696, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 21703, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 21710, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 21717, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 21728, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 21736, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 21744, 7);
}


void RXX_Location_Loc107_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 21419, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 21440, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 21429, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 21456, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 21408, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 21392, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 21376, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 21449, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 21552, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 21472, 11);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 21518, 7);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 21483, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 21536, 8);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 21525, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 21544, 8);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 21511, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 21504, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 21493, 9);
}


void RXX_Location_Loc106_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 21264, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 21184, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 21200, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 21216, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 21227, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 21237, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 21248, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 21257, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 21280, 11);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 21352, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 21301, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 21312, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 21319, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 21326, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 21333, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 21344, 8);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 21291, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 21360, 7);
}


void RXX_Location_Loc105_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 21035, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 21056, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 21045, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 21072, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 21024, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 21008, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 20992, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 21065, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 21088, 11);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 21160, 8);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 21134, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 21168, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 21152, 8);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 21141, 10);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 21099, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 21127, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 21120, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray,
 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 9024, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 9072, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 9013, 9);
}


void RXX_Location_Loc41_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 8784, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 8704, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 8720, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 8736, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 8747, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 8757, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 8768, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 8777, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 8800, 11);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 8872, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 8821, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 8832, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 8839, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 8846, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 8853, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 8864, 8);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 8811, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 8880, 7);
}


void RXX_Location_Loc40_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 8555, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 8576, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 8565, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 8592, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 8544, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 8528, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 8512, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 8585, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 8629, 9);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 8608, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 8672, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 8688, 7);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 8619, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 8680, 8);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 8640, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 8661, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 8654, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 8647, 7);
}


void RXX_Location_Loc39_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 8320, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 8336, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 8352, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 8363, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 8373, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 8384, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 8393, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 8400, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 8416, 11);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 8427, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 8437, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 8455, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 8462, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 8469, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 8480, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 8488, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 8496, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 8448, 7);
}


void RXX_Location_Loc38_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 8181, 10);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 8160, 11);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 8192, 9);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 8171, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 8277, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 8144, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 8128, 16);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 8208, 14);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 8201, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 8304, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 8224, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 8245, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 8256, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 8263, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 8270, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 8288, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 8296, 8);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 8235, 10);
}


void RXX_Location_Loc37_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 8032, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 7936, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 7952, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 7968, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 7979, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 7989, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 8000, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 8009, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 8016, 14);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 8104, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 8053, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 8064, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 8071, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 8078, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 8085, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 8096, 8);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 8043, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 8112, 7);
}


void RXX_Location_Loc36_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 7787, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 7808, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 7797, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 7840, 11);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 7776, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 7760, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 7744, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 7817, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 7824, 14);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 7912, 8);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 7886, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 7920, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 7904, 8);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 7893, 10);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 7851, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 7879, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 7872, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 7861, 9);
}


void RXX_Location_Loc35_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 7632, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 7552, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 7568, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 7584, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 7595, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 7605, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 7616, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 7625, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 7648, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 7669, 9);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 7659, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 7680, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 7687, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 7694, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 7701, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 7712, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 7720, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 7728, 7);
}


void RXX_Location_Loc34_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 7403, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 7424, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 7413, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 7440, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 7392, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 7376, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 7360, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 7433, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 7528, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 7456, 11);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 7502, 7);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 7467, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 7520, 8);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 7509, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 7536, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 7495, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 7488, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 7477, 9);
}


void RXX_Location_Loc33_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 7248, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 7168, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 7184, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 7200, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 7211, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 7221, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 7232, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 7241, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 7264, 11);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 7336, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 7285, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 7296, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 7303, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 7310, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 7317, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 7328, 8);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 7275, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 7344, 7);
}


void RXX_Location_Loc32_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 7019, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 7040, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 7029, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 7056, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 7008, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 6992, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 6976, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 7049, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 7152, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 7072, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 7093, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 7104, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 7111, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 7118, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 7125, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 7136, 8);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 7083, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 7144, 8);
}


void RXX_Location_Loc31_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 6837, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 6857, 7);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 6848, 9);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 6784, 16);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 6827, 10);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 6816, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 6800, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 6880, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 6864, 14);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 6891, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 6944, 8);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 6912, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 6960, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 6952, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 6901, 9);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 6933, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 6926, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 6919, 7);
}


void RXX_Location_Loc30_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 6688, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 6592, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 6608, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 6624, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 6635, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 6645, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 6665, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 6672, 14);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 6656, 9);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 6699, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 6741, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 6768, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 6709, 9);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 6752, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 6760, 8);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 6734, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 6727, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 6720, 7);
}


void RXX_Location_Loc29_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 6496, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 6400, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 6416, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 6432, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 6443, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 6453, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 6473, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 6480, 14);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 6464, 9);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 6507, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 6542, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 6517, 9);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 6560, 8);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 6549, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 6568, 8);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 6535, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 6528, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 6576, 7);
}


void RXX_Location_Loc28_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 6208, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 6224, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 6240, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 6251, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 6261, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 6272, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 6281, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 6288, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 6304, 11);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 6315, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 6357, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 6325, 9);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 6376, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 6368, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 6384, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 6350, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 6343, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 6336, 7);
}


void RXX_Location_Loc27_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 6112, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 6016, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 6032, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 6048, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 6059, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 6069, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 6089, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 6096, 14);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 6080, 9);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 6123, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 6158, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 6133, 9);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 6176, 8);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 6165, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 6184, 8);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 6151, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 6144, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 6192, 7);
}


void RXX_Location_Loc26_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 5824, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 5840, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 5856, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 5867, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 5877, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 5888, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 5897, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 5904, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 5920, 11);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 5931, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 5973, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 5941, 9);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 5992, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 5984, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 6000, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 5966, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 5959, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 5952, 7);
}


void RXX_Location_Loc25_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 5632, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 5648, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 5664, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 5675, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 5685, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 5696, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 5705, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 5712, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 5728, 11);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 5739, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 5781, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 5749, 9);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 5800, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 5792, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 5808, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 5774, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 5767, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 5760, 7);
}


void RXX_Location_Loc24_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 5440, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 5456, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 5472, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 5483, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 5493, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 5504, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 5513, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 5520, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 5536, 11);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 5547, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 5589, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 5557, 9);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 5608, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 5600, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 5616, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 5582, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 5575, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 5568, 7);
}


void RXX_Location_Loc23_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 5328, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 5248, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 5264, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 5280, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 5291, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 5301, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 5312, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 5321, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 5397, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 5344, 11);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 5390, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 5416, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 5408, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 5424, 7);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 5355, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 5383, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 5376, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 5365, 9);
}


void RXX_Location_Loc22_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 5136, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 5056, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 5072, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 5088, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 5099, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 5109, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 5120, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 5129, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 5152, 11);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 5224, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 5173, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 5184, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 5191, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 5198, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 5205, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 5216, 8);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 5163, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 5232, 7);
}


void RXX_Location_Loc21_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 4907, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 4928, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 4917, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 4944, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 4896, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 4880, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 4864, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 4937, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 4960, 11);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 5032, 8);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 5006, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 5040, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 5024, 8);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 5013, 10);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 4971, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 4999, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 4992, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 4981, 9);
}


void RXX_Location_Loc20_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 4752, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 4672, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 4688, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 4704, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 4715, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 4725, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 4736, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 4745, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 4768, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 4789, 9);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 4779, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 4800, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 4807, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 4814, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 4821, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 4832, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 4840, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 4848, 7);
}


void RXX_Location_Loc19_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 4523, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 4544, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 4533, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 4560, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 4512, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 4496, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 4480, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 4553, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 4656, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 4576, 11);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 4622, 7);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 4587, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 4640, 8);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 4629, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 4648, 8);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 4615, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 4608, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 4597, 9);
}


void RXX_Location_Loc18_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 4368, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 4288, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 4304, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 4320, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 4331, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 4341, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 4352, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 4361, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 4384, 11);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 4456, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 4405, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 4416, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 4423, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 4430, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 4437, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 4448, 8);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 4395, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 4464, 7);
}


void RXX_Location_Loc17_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 4139, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 4160, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 4149, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 4176, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 4128, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 4112, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 4096, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 4169, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 4272, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 4192, 11);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 4238, 7);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 4203, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 4256, 8);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 4245, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 4264, 8);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 4231, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 4224, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 4213, 9);
}


void RXX_Location_Loc16_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 3904, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 4000, 11);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 3936, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 3947, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 3957, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 3968, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 3977, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 3984, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 3920, 15);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 4011, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 4053, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 4080, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 4072, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 4064, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 4021, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 4046, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 4039, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 4032, 7);
}


void RXX_Location_Loc15_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 3792, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 3712, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 3728, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 3744, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 3755, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 3765, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 3776, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 3785, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 3880, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 3808, 11);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 3854, 7);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 3819, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 3872, 8);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 3861, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 3888, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 3847, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 3840, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 3829, 9);
}


void RXX_Location_Loc14_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 3600, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 3520, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 3536, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 3552, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 3563, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 3573, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 3584, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 3593, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 3616, 11);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 3688, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 3637, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 3648, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 3655, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 3662, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 3669, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 3680, 8);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 3627, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 3696, 7);
}


void RXX_Location_Loc13_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 3371, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 3392, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 3381, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 3408, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 3360, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 3344, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 3328, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 3401, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 3424, 11);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 3496, 8);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 3470, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 3504, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 3488, 8);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 3477, 10);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 3435, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 3463, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 3456, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 3445, 9);
}


void RXX_Location_Loc12_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 3216, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 3136, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 3152, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 3168, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 3179, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 3189, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 3200, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 3209, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 3232, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 3253, 9);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 3243, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 3264, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 3271, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 3278, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 3285, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 3296, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 3304, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 3312, 7);
}


void RXX_Location_Loc11_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 2987, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 3008, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 2997, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 3024, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 2976, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 2960, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 2944, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 3017, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 3120, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 3040, 11);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 3086, 7);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 3051, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 3104, 8);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 3093, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 3112, 8);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 3079, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 3072, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 3061, 9);
}


void RXX_Location_Loc10_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 2832, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 2752, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 2768, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 2784, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 2795, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 2805, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 2816, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 2825, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 2848, 11);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 2920, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 2869, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 2880, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 2887, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 2894, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 2901, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 2912, 8);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 2859, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 2928, 7);
}


void RXX_Location_Loc9_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 2603, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 2624, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 2613, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 2640, 14);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 2592, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 2576, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 2560, 16);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 2633, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 2736, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 2656, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 2677, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 2688, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 2695, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 2702, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 2709, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 2720, 8);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 2667, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 2728, 8);
}


void RXX_Location_Loc8_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 2432, 9);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 2464, 11);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 2441, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 2368, 16);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 2421, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 2411, 10);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 2400, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 2448, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 2384, 15);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 2475, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 2528, 8);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 2496, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 2544, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 2536, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 2485, 9);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 2517, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 2503, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 2510, 7);
}


void RXX_Location_Loc7_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 2344, 8);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 2249, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 2336, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 2352, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 2176, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 2192, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 2208, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 2219, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 2240, 9);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 2229, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 2256, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 2272, 11);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 2283, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 2293, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 2304, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 2311, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 2318, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 2325, 10);
}


void RXX_Location_Loc6_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 2080, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 1984, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 2000, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 2016, 11);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 2037, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 2057, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 2064, 14);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 2027, 10);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 2091, 10);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 2133, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 2160, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 2101, 9);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 2152, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 2144, 8);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 2048, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 2126, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 2112, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 2119, 7);
}


void RXX_Location_Loc5_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 1872, 14);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 1792, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 1808, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 1824, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 1835, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 1845, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 1865, 7);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 1856, 9);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 1888, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 1909, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 1920, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 1927, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 1934, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 1941, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 1952, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 1960, 8);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 1899, 10);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 1968, 7);
}


void RXX_Location_Loc4_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 1653, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 1673, 7);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 1664, 9);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 1696, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 1643, 10);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 1632, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 1616, 15);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 1600, 16);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 1680, 14);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 1707, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 1760, 8);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 1728, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 1776, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 1768, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 1717, 9);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 1749, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 1735, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 1742, 7);
}


void RXX_Location_Loc3_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 1576, 8);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 1488, 14);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 1584, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 1408, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 1424, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 1440, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 1568, 8);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 1461, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 1481, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 1451, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 1504, 11);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 1515, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 1525, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 1536, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 1543, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 1550, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 1557, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 1472, 9);
}


void RXX_Location_Loc2_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 1312, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 1216, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 1232, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 1248, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 1259, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 1269, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 1280, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 1289, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 1296, 14);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 1323, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 1376, 8);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 1344, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 1392, 7);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 1384, 8);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 1333, 9);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 1365, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 1351, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 1358, 7);
}


void RXX_Location_Loc1_write(byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    uint64_t raw_value;

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].azimuth_angle, 0.00175, -1.792);
    copyBitsToByteArrayLE(raw_value, byteArray, 1120, 11);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_distance, 0.01, 0.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 1024, 16);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radial_velocity, 0.01, -150.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 1040, 15);

    raw_value = 119;
    copyBitsToByteArrayLE(raw_value, byteArray, 1056, 11);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 1067, 10);

    raw_value = 489;
    copyBitsToByteArrayLE(raw_value, byteArray, 1077, 10);

    raw_value = 30;
    copyBitsToByteArrayLE(raw_value, byteArray, 1088, 9);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 1097, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 1104, 14);

    raw_value = 500;
    copyBitsToByteArrayLE(raw_value, byteArray, 1131, 10);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 1152, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 1159, 7);

    raw_value = 100;
    copyBitsToByteArrayLE(raw_value, byteArray, 1166, 7);

    raw_value = 0;
    copyBitsToByteArrayLE(raw_value, byteArray, 1173, 10);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].radar_cross_section, 0.5, -50.0);
    copyBitsToByteArrayLE(raw_value, byteArray, 1184, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 1192, 8);

    raw_value = 1;
    copyBitsToByteArrayLE(raw_value, byteArray, 1200, 7);

    raw_value = convertPhysicalToRaw(instance->calculation_class.location_cloud[locIndex].elevation_angle, 0.00175, -0.448);
    copyBitsToByteArrayLE(raw_value, byteArray, 1141, 9);
}


void Write_ByteArray_RXX_Location(int64 locationPosInStruct, byte byteArray[], int64 locIndex, CaplInstanceData *instance)
{
    switch(locationPosInStruct)
    {
    case 500:
    RXX_Location_Loc500_write(byteArray, locIndex, instance);break;
    case 499:
    RXX_Location_Loc499_write(byteArray, locIndex, instance);break;
    case 498:
    RXX_Location_Loc498_write(byteArray, locIndex, instance);break;
    case 497:
    RXX_Location_Loc497_write(byteArray, locIndex, instance);break;
    case 496:
    RXX_Location_Loc496_write(byteArray, locIndex, instance);break;
    case 495:
    RXX_Location_Loc495_write(byteArray, locIndex, instance);break;
    case 494:
    RXX_Location_Loc494_write(byteArray, locIndex, instance);break;
    case 493:
    RXX_Location_Loc493_write(byteArray, locIndex, instance);break;
    case 492:
    RXX_Location_Loc492_write(byteArray, locIndex, instance);break;
    case 491:
    RXX_Location_Loc491_write(byteArray, locIndex, instance);break;
    case 490:
    RXX_Location_Loc490_write(byteArray, locIndex, instance);break;
    case 489:
    RXX_Location_Loc489_write(byteArray, locIndex, instance);break;
    case 488:
    RXX_Location_Loc488_write(byteArray, locIndex, instance);break;
    case 487:
    RXX_Location_Loc487_write(byteArray, locIndex, instance);break;
    case 486:
    RXX_Location_Loc486_write(byteArray, locIndex, instance);break;
    case 485:
    RXX_Location_Loc485_write(byteArray, locIndex, instance);break;
    case 484:
    RXX_Location_Loc484_write(byteArray, locIndex, instance);break;
    case 483:
    RXX_Location_Loc483_write(byteArray, locIndex, instance);break;
    case 482:
    RXX_Location_Loc482_write(byteArray, locIndex, instance);break;
    case 481:
    RXX_Location_Loc481_write(byteArray, locIndex, instance);break;
    case 480:
    RXX_Location_Loc480_write(byteArray, locIndex, instance);break;
    case 479:
    RXX_Location_Loc479_write(byteArray, locIndex, instance);break;
    case 478:
    RXX_Location_Loc478_write(byteArray, locIndex, instance);break;
    case 477:
    RXX_Location_Loc477_write(byteArray, locIndex, instance);break;
    case 476:
    RXX_Location_Loc476_write(byteArray, locIndex, instance);break;
    case 475:
    RXX_Location_Loc475_write(byteArray, locIndex, instance);break;
    case 474:
    RXX_Location_Loc474_write(byteArray, locIndex, instance);break;
    case 473:
    RXX_Location_Loc473_write(byteArray, locIndex, instance);break;
    case 472:
    RXX_Location_Loc472_write(byteArray, locIndex, instance);break;
    case 471:
    RXX_Location_Loc471_write(byteArray, locIndex, instance);break;
    case 470:
    RXX_Location_Loc470_write(byteArray, locIndex, instance);break;
    case 469:
    RXX_Location_Loc469_write(byteArray, locIndex, instance);break;
    case 468:
    RXX_Location_Loc468_write(byteArray, locIndex, instance);break;
    case 467:
    RXX_Location_Loc467_write(byteArray, locIndex, instance);break;
    case 466:
    RXX_Location_Loc466_write(byteArray, locIndex, instance);break;
    case 465:
    RXX_Location_Loc465_write(byteArray, locIndex, instance);break;
    case 464:
    RXX_Location_Loc464_write(byteArray, locIndex, instance);break;
    case 463:
    RXX_Location_Loc463_write(byteArray, locIndex, instance);break;
    case 462:
    RXX_Location_Loc462_write(byteArray, locIndex, instance);break;
    case 461:
    RXX_Location_Loc461_write(byteArray, locIndex, instance);break;
    case 460:
    RXX_Location_Loc460_write(byteArray, locIndex, instance);break;
    case 459:
    RXX_Location_Loc459_write(byteArray, locIndex, instance);break;
    case 458:
    RXX_Location_Loc458_write(byteArray, locIndex, instance);break;
    case 457:
    RXX_Location_Loc457_write(byteArray, locIndex, instance);break;
    case 456:
    RXX_Location_Loc456_write(byteArray, locIndex, instance);break;
    case 455:
    RXX_Location_Loc455_write(byteArray, locIndex, instance);break;
    case 454:
    RXX_Location_Loc454_write(byteArray, locIndex, instance);break;
    case 453:
    RXX_Location_Loc453_write(byteArray, locIndex, instance);break;
    case 452:
    RXX_Location_Loc452_write(byteArray, locIndex, instance);break;
    case 451:
    RXX_Location_Loc451_write(byteArray, locIndex, instance);break;
    case 450:
    RXX_Location_Loc450_write(byteArray, locIndex, instance);break;
    case 449:
    RXX_Location_Loc449_write(byteArray, locIndex, instance);break;
    case 448:
    RXX_Location_Loc448_write(byteArray, locIndex, instance);break;
    case 447:
    RXX_Location_Loc447_write(byteArray, locIndex, instance);break;
    case 446:
    RXX_Location_Loc446_write(byteArray, locIndex, instance);break;
    case 445:
    RXX_Location_Loc445_write(byteArray, locIndex, instance);break;
    case 444:
    RXX_Location_Loc444_write(byteArray, locIndex, instance);break;
    case 443:
    RXX_Location_Loc443_write(byteArray, locIndex, instance);break;
    case 442:
    RXX_Location_Loc442_write(byteArray, locIndex, instance);break;
    case 441:
    RXX_Location_Loc441_write(byteArray, locIndex, instance);break;
    case 440:
    RXX_Location_Loc440_write(byteArray, locIndex, instance);break;
    case 439:
    RXX_Location_Loc439_write(byteArray, locIndex, instance);break;
    case 438:
    RXX_Location_Loc438_write(byteArray, locIndex, instance);break;
    case 437:
    RXX_Location_Loc437_write(byteArray, locIndex, instance);break;
    case 436:
    RXX_Location_Loc436_write(byteArray, locIndex, instance);break;
    case 435:
    RXX_Location_Loc435_write(byteArray, locIndex, instance);break;
    case 434:
    RXX_Location_Loc434_write(byteArray, locIndex, instance);break;
    case 433:
    RXX_Location_Loc433_write(byteArray, locIndex, instance);break;
    case 432:
    RXX_Location_Loc432_write(byteArray, locIndex, instance);break;
    case 431:
    RXX_Location_Loc431_write(byteArray, locIndex, instance);break;
    case 430:
    RXX_Location_Loc430_write(byteArray, locIndex, instance);break;
    case 429:
    RXX_Location_Loc429_write(byteArray, locIndex, instance);break;
    case 428:
    RXX_Location_Loc428_write(byteArray, locIndex, instance);break;
    case 427:
    RXX_Location_Loc427_write(byteArray, locIndex, instance);break;
    case 426:
    RXX_Location_Loc426_write(byteArray, locIndex, instance);break;
    case 425:
    RXX_Location_Loc425_write(byteArray, locIndex, instance);break;
    case 424:
    RXX_Location_Loc424_write(byteArray, locIndex, instance);break;
    case 423:
    RXX_Location_Loc423_write(byteArray, locIndex, instance);break;
    case 422:
    RXX_Location_Loc422_write(byteArray, locIndex, instance);break;
    case 421:
    RXX_Location_Loc421_write(byteArray, locIndex, instance);break;
    case 420:
    RXX_Location_Loc420_write(byteArray, locIndex, instance);break;
    case 419:
    RXX_Location_Loc419_write(byteArray, locIndex, instance);break;
    case 418:
    RXX_Location_Loc418_write(byteArray, locIndex, instance);break;
    case 417:
    RXX_Location_Loc417_write(byteArray, locIndex, instance);break;
    case 416:
    RXX_Location_Loc416_write(byteArray, locIndex, instance);break;
    case 415:
    RXX_Location_Loc415_write(byteArray, locIndex, instance);break;
    case 414:
    RXX_Location_Loc414_write(byteArray, locIndex, instance);break;
    case 413:
    RXX_Location_Loc413_write(byteArray, locIndex, instance);break;
    case 412:
    RXX_Location_Loc412_write(byteArray, locIndex, instance);break;
    case 411:
    RXX_Location_Loc411_write(byteArray, locIndex, instance);break;
    case 410:
    RXX_Location_Loc410_write(byteArray, locIndex, instance);break;
    case 409:
    RXX_Location_Loc409_write(byteArray, locIndex, instance);break;
    case 408:
    RXX_Location_Loc408_write(byteArray, locIndex, instance);break;
    case 407:
    RXX_Location_Loc407_write(byteArray, locIndex, instance);break;
    case 406:
    RXX_Location_Loc406_write(byteArray, locIndex, instance);break;
    case 405:
    RXX_Location_Loc405_write(byteArray, locIndex, instance);break;
    case 404:
    RXX_Location_Loc404_write(byteArray, locIndex, instance);break;
    case 403:
    RXX_Location_Loc403_write(byteArray, locIndex, instance);break;
    case 402:
    RXX_Location_Loc402_write(byteArray, locIndex, instance);break;
    case 401:
    RXX_Location_Loc401_write(byteArray, locIndex, instance);break;
    case 400:
    RXX_Location_Loc400_write(byteArray, locIndex, instance);break;
    case 399:
    RXX_Location_Loc399_write(byteArray, locIndex, instance);break;
    case 398:
    RXX_Location_Loc398_write(byteArray, locIndex, instance);break;
    case 397:
    RXX_Location_Loc397_write(byteArray, locIndex, instance);break;
    case 396:
    RXX_Location_Loc396_write(byteArray, locIndex, instance);break;
    case 395:
    RXX_Location_Loc395_write(byteArray, locIndex, instance);break;
    case 394:
    RXX_Location_Loc394_write(byteArray, locIndex, instance);break;
    case 393:
    RXX_Location_Loc393_write(byteArray, locIndex, instance);break;
    case 392:
    RXX_Location_Loc392_write(byteArray, locIndex, instance);break;
    case 391:
    RXX_Location_Loc391_write(byteArray, locIndex, instance);break;
    case 390:
    RXX_Location_Loc390_write(byteArray, locIndex, instance);break;
    case 389:
    RXX_Location_Loc389_write(byteArray, locIndex, instance);break;
    case 388:
    RXX_Location_Loc388_write(byteArray, locIndex, instance);break;
    case 387:
    RXX_Location_Loc387_write(byteArray, locIndex, instance);break;
    case 386:
    RXX_Location_Loc386_write(byteArray, locIndex, instance);break;
    case 385:
    RXX_Location_Loc385_write(byteArray, locIndex, instance);break;
    case 384:
    RXX_Location_Loc384_write(byteArray, locIndex, instance);break;
    case 383:
    RXX_Location_Loc383_write(byteArray, locIndex, instance);break;
    case 382:
    RXX_Location_Loc382_write(byteArray, locIndex, instance);break;
    case 381:
    RXX_Location_Loc381_write(byteArray, locIndex, instance);break;
    case 380:
    RXX_Location_Loc380_write(byteArray, locIndex, instance);break;
    case 379:
    RXX_Location_Loc379_write(byteArray, locIndex, instance);break;
    case 378:
    RXX_Location_Loc378_write(byteArray, locIndex, instance);break;
    case 377:
    RXX_Location_Loc377_write(byteArray, locIndex, instance);break;
    case 376:
    RXX_Location_Loc376_write(byteArray, locIndex, instance);break;
    case 375:
    RXX_Location_Loc375_write(byteArray, locIndex, instance);break;
    case 374:
    RXX_Location_Loc374_write(byteArray, locIndex, instance);break;
    case 373:
    RXX_Location_Loc373_write(byteArray, locIndex, instance);break;
    case 372:
    RXX_Location_Loc372_write(byteArray, locIndex, instance);break;
    case 371:
    RXX_Location_Loc371_write(byteArray, locIndex, instance);break;
    case 370:
    RXX_Location_Loc370_write(byteArray, locIndex, instance);break;
    case 369:
    RXX_Location_Loc369_write(byteArray, locIndex, instance);break;
    case 368:
    RXX_Location_Loc368_write(byteArray, locIndex, instance);break;
    case 367:
    RXX_Location_Loc367_write(byteArray, locIndex, instance);break;
    case 366:
    RXX_Location_Loc366_write(byteArray, locIndex, instance);break;
    case 365:
    RXX_Location_Loc365_write(byteArray, locIndex, instance);break;
    case 364:
    RXX_Location_Loc364_write(byteArray, locIndex, instance);break;
    case 363:
    RXX_Location_Loc363_write(byteArray, locIndex, instance);break;
    case 362:
    RXX_Location_Loc362_write(byteArray, locIndex, instance);break;
    case 361:
    RXX_Location_Loc361_write(byteArray, locIndex, instance);break;
    case 360:
    RXX_Location_Loc360_write(byteArray, locIndex, instance);break;
    case 359:
    RXX_Location_Loc359_write(byteArray, locIndex, instance);break;
    case 358:
    RXX_Location_Loc358_write(byteArray, locIndex, instance);break;
    case 357:
    RXX_Location_Loc357_write(byteArray, locIndex, instance);break;
    case 356:
    RXX_Location_Loc356_write(byteArray, locIndex, instance);break;
    case 355:
    RXX_Location_Loc355_write(byteArray, locIndex, instance);break;
    case 354:
    RXX_Location_Loc354_write(byteArray, locIndex, instance);break;
    case 353:
    RXX_Location_Loc353_write(byteArray, locIndex, instance);break;
    case 352:
    RXX_Location_Loc352_write(byteArray, locIndex, instance);break;
    case 351:
    RXX_Location_Loc351_write(byteArray, locIndex, instance);break;
    case 350:
    RXX_Location_Loc350_write(byteArray, locIndex, instance);break;
    case 349:
    RXX_Location_Loc349_write(byteArray, locIndex, instance);break;
    case 348:
    RXX_Location_Loc348_write(byteArray, locIndex, instance);break;
    case 347:
    RXX_Location_Loc347_write(byteArray, locIndex, instance);break;
    case 346:
    RXX_Location_Loc346_write(byteArray, locIndex, instance);break;
    case 345:
    RXX_Location_Loc345_write(byteArray, locIndex, instance);break;
    case 344:
    RXX_Location_Loc344_write(byteArray, locIndex, instance);break;
    case 343:
    RXX_Location_Loc343_write(byteArray, locIndex, instance);break;
    case 342:
    RXX_Location_Loc342_write(byteArray, locIndex, instance);break;
    case 341:
    RXX_Location_Loc341_write(byteArray, locIndex, instance);break;
    case 340:
    RXX_Location_Loc340_write(byteArray, locIndex, instance);break;
    case 339:
    RXX_Location_Loc339_write(byteArray, locIndex, instance);break;
    case 338:
    RXX_Location_Loc338_write(byteArray, locIndex, instance);break;
    case 337:
    RXX_Location_Loc337_write(byteArray, locIndex, instance);break;
    case 336:
    RXX_Location_Loc336_write(byteArray, locIndex, instance);break;
    case 335:
    RXX_Location_Loc335_write(byteArray, locIndex, instance);break;
    case 334:
    RXX_Location_Loc334_write(byteArray, locIndex, instance);break;
    case 333:
    RXX_Location_Loc333_write(byteArray, locIndex, instance);break;
    case 332:
    RXX_Location_Loc332_write(byteArray, locIndex, instance);break;
    case 331:
    RXX_Location_Loc331_write(byteArray, locIndex, instance);break;
    case 330:
    RXX_Location_Loc330_write(byteArray, locIndex, instance);break;
    case 329:
    RXX_Location_Loc329_write(byteArray, locIndex, instance);break;
    case 328:
    RXX_Location_Loc328_write(byteArray, locIndex, instance);break;
    case 327:
    RXX_Location_Loc327_write(byteArray, locIndex, instance);break;
    case 326:
    RXX_Location_Loc326_write(byteArray, locIndex, instance);break;
    case 325:
    RXX_Location_Loc325_write(byteArray, locIndex, instance);break;
    case 324:
    RXX_Location_Loc324_write(byteArray, locIndex, instance);break;
    case 323:
    RXX_Location_Loc323_write(byteArray, locIndex, instance);break;
    case 322:
    RXX_Location_Loc322_write(byteArray, locIndex, instance);break;
    case 321:
    RXX_Location_Loc321_write(byteArray, locIndex, instance);break;
    case 320:
    RXX_Location_Loc320_write(byteArray, locIndex, instance);break;
    case 319:
    RXX_Location_Loc319_write(byteArray, locIndex, instance);break;
    case 318:
    RXX_Location_Loc318_write(byteArray, locIndex, instance);break;
    case 317:
    RXX_Location_Loc317_write(byteArray, locIndex, instance);break;
    case 316:
    RXX_Location_Loc316_write(byteArray, locIndex, instance);break;
    case 315:
    RXX_Location_Loc315_write(byteArray, locIndex, instance);break;
    case 314:
    RXX_Location_Loc314_write(byteArray, locIndex, instance);break;
    case 313:
    RXX_Location_Loc313_write(byteArray, locIndex, instance);break;
    case 312:
    RXX_Location_Loc312_write(byteArray, locIndex, instance);break;
    case 311:
    RXX_Location_Loc311_write(byteArray, locIndex, instance);break;
    case 310:
    RXX_Location_Loc310_write(byteArray, locIndex, instance);break;
    case 309:
    RXX_Location_Loc309_write(byteArray, locIndex, instance);break;
    case 308:
    RXX_Location_Loc308_write(byteArray, locIndex, instance);break;
    case 307:
    RXX_Location_Loc307_write(byteArray, locIndex, instance);break;
    case 306:
    RXX_Location_Loc306_write(byteArray, locIndex, instance);break;
    case 305:
    RXX_Location_Loc305_write(byteArray, locIndex, instance);break;
    case 304:
    RXX_Location_Loc304_write(byteArray, locIndex, instance);break;
    case 303:
    RXX_Location_Loc303_write(byteArray, locIndex, instance);break;
    case 302:
    RXX_Location_Loc302_write(byteArray, locIndex, instance);break;
    case 301:
    RXX_Location_Loc301_write(byteArray, locIndex, instance);break;
    case 300:
    RXX_Location_Loc300_write(byteArray, locIndex, instance);break;
    case 299:
    RXX_Location_Loc299_write(byteArray, locIndex, instance);break;
    case 298:
    RXX_Location_Loc298_write(byteArray, locIndex, instance);break;
    case 297:
    RXX_Location_Loc297_write(byteArray, locIndex, instance);break;
    case 296:
    RXX_Location_Loc296_write(byteArray, locIndex, instance);break;
    case 295:
    RXX_Location_Loc295_write(byteArray, locIndex, instance);break;
    case 294:
    RXX_Location_Loc294_write(byteArray, locIndex, instance);break;
    case 293:
    RXX_Location_Loc293_write(byteArray, locIndex, instance);break;
    case 292:
    RXX_Location_Loc292_write(byteArray, locIndex, instance);break;
    case 291:
    RXX_Location_Loc291_write(byteArray, locIndex, instance);break;
    case 290:
    RXX_Location_Loc290_write(byteArray, locIndex, instance);break;
    case 289:
    RXX_Location_Loc289_write(byteArray, locIndex, instance);break;
    case 288:
    RXX_Location_Loc288_write(byteArray, locIndex, instance);break;
    case 287:
    RXX_Location_Loc287_write(byteArray, locIndex, instance);break;
    case 286:
    RXX_Location_Loc286_write(byteArray, locIndex, instance);break;
    case 285:
    RXX_Location_Loc285_write(byteArray, locIndex, instance);break;
    case 284:
    RXX_Location_Loc284_write(byteArray, locIndex, instance);break;
    case 283:
    RXX_Location_Loc283_write(byteArray, locIndex, instance);break;
    case 282:
    RXX_Location_Loc282_write(byteArray, locIndex, instance);break;
    case 281:
    RXX_Location_Loc281_write(byteArray, locIndex, instance);break;
    case 280:
    RXX_Location_Loc280_write(byteArray, locIndex, instance);break;
    case 279:
    RXX_Location_Loc279_write(byteArray, locIndex, instance);break;
    case 278:
    RXX_Location_Loc278_write(byteArray, locIndex, instance);break;
    case 277:
    RXX_Location_Loc277_write(byteArray, locIndex, instance);break;
    case 276:
    RXX_Location_Loc276_write(byteArray, locIndex, instance);break;
    case 275:
    RXX_Location_Loc275_write(byteArray, locIndex, instance);break;
    case 274:
    RXX_Location_Loc274_write(byteArray, locIndex, instance);break;
    case 273:
    RXX_Location_Loc273_write(byteArray, locIndex, instance);break;
    case 272:
    RXX_Location_Loc272_write(byteArray, locIndex, instance);break;
    case 271:
    RXX_Location_Loc271_write(byteArray, locIndex, instance);break;
    case 270:
    RXX_Location_Loc270_write(byteArray, locIndex, instance);break;
    case 269:
    RXX_Location_Loc269_write(byteArray, locIndex, instance);break;
    case 268:
    RXX_Location_Loc268_write(byteArray, locIndex, instance);break;
    case 267:
    RXX_Location_Loc267_write(byteArray, locIndex, instance);break;
    case 266:
    RXX_Location_Loc266_write(byteArray, locIndex, instance);break;
    case 265:
    RXX_Location_Loc265_write(byteArray, locIndex, instance);break;
    case 264:
    RXX_Location_Loc264_write(byteArray, locIndex, instance);break;
    case 263:
    RXX_Location_Loc263_write(byteArray, locIndex, instance);break;
    case 262:
    RXX_Location_Loc262_write(byteArray, locIndex, instance);break;
    case 261:
    RXX_Location_Loc261_write(byteArray, locIndex, instance);break;
    case 260:
    RXX_Location_Loc260_write(byteArray, locIndex, instance);break;
    case 259:
    RXX_Location_Loc259_write(byteArray, locIndex, instance);break;
    case 258:
    RXX_Location_Loc258_write(byteArray, locIndex, instance);break;
    case 257:
    RXX_Location_Loc257_write(byteArray, locIndex, instance);break;
    case 256:
    RXX_Location_Loc256_write(byteArray, locIndex, instance);break;
    case 255:
    RXX_Location_Loc255_write(byteArray, locIndex, instance);break;
    case 254:
    RXX_Location_Loc254_write(byteArray, locIndex, instance);break;
    case 253:
    RXX_Location_Loc253_write(byteArray, locIndex, instance);break;
    case 252:
    RXX_Location_Loc252_write(byteArray, locIndex, instance);break;
    case 251:
    RXX_Location_Loc251_write(byteArray, locIndex, instance);break;
    case 250:
    RXX_Location_Loc250_write(byteArray, locIndex, instance);break;
    case 249:
    RXX_Location_Loc249_write(byteArray, locIndex, instance);break;
    case 248:
    RXX_Location_Loc248_write(byteArray, locIndex, instance);break;
    case 247:
    RXX_Location_Loc247_write(byteArray, locIndex, instance);break;
    case 246:
    RXX_Location_Loc246_write(byteArray, locIndex, instance);break;
    case 245:
    RXX_Location_Loc245_write(byteArray, locIndex, instance);break;
    case 244:
    RXX_Location_Loc244_write(byteArray, locIndex, instance);break;
    case 243:
    RXX_Location_Loc243_write(byteArray, locIndex, instance);break;
    case 242:
    RXX_Location_Loc242_write(byteArray, locIndex, instance);break;
    case 241:
    RXX_Location_Loc241_write(byteArray, locIndex, instance);break;
    case 240:
    RXX_Location_Loc240_write(byteArray, locIndex, instance);break;
    case 239:
    RXX_Location_Loc239_write(byteArray, locIndex, instance);break;
    case 238:
    RXX_Location_Loc238_write(byteArray, locIndex, instance);break;
    case 237:
    RXX_Location_Loc237_write(byteArray, locIndex, instance);break;
    case 236:
    RXX_Location_Loc236_write(byteArray, locIndex, instance);break;
    case 235:
    RXX_Location_Loc235_write(byteArray, locIndex, instance);break;
    case 234:
    RXX_Location_Loc234_write(byteArray, locIndex, instance);break;
    case 233:
    RXX_Location_Loc233_write(byteArray, locIndex, instance);break;
    case 232:
    RXX_Location_Loc232_write(byteArray, locIndex, instance);break;
    case 231:
    RXX_Location_Loc231_write(byteArray, locIndex, instance);break;
    case 230:
    RXX_Location_Loc230_write(byteArray, locIndex, instance);break;
    case 229:
    RXX_Location_Loc229_write(byteArray, locIndex, instance);break;
    case 228:
    RXX_Location_Loc228_write(byteArray, locIndex, instance);break;
    case 227:
    RXX_Location_Loc227_write(byteArray, locIndex, instance);break;
    case 226:
    RXX_Location_Loc226_write(byteArray, locIndex, instance);break;
    case 225:
    RXX_Location_Loc225_write(byteArray, locIndex, instance);break;
    case 224:
    RXX_Location_Loc224_write(byteArray, locIndex, instance);break;
    case 223:
    RXX_Location_Loc223_write(byteArray, locIndex, instance);break;
    case 222:
    RXX_Location_Loc222_write(byteArray, locIndex, instance);break;
    case 221:
    RXX_Location_Loc221_write(byteArray, locIndex, instance);break;
    case 220:
    RXX_Location_Loc220_write(byteArray, locIndex, instance);break;
    case 219:
    RXX_Location_Loc219_write(byteArray, locIndex, instance);break;
    case 218:
    RXX_Location_Loc218_write(byteArray, locIndex, instance);break;
    case 217:
    RXX_Location_Loc217_write(byteArray, locIndex, instance);break;
    case 216:
    RXX_Location_Loc216_write(byteArray, locIndex, instance);break;
    case 215:
    RXX_Location_Loc215_write(byteArray, locIndex, instance);break;
    case 214:
    RXX_Location_Loc214_write(byteArray, locIndex, instance);break;
    case 213:
    RXX_Location_Loc213_write(byteArray, locIndex, instance);break;
    case 212:
    RXX_Location_Loc212_write(byteArray, locIndex, instance);break;
    case 211:
    RXX_Location_Loc211_write(byteArray, locIndex, instance);break;
    case 210:
    RXX_Location_Loc210_write(byteArray, locIndex, instance);break;
    case 209:
    RXX_Location_Loc209_write(byteArray, locIndex, instance);break;
    case 208:
    RXX_Location_Loc208_write(byteArray, locIndex, instance);break;
    case 207:
    RXX_Location_Loc207_write(byteArray, locIndex, instance);break;
    case 206:
    RXX_Location_Loc206_write(byteArray, locIndex, instance);break;
    case 205:
    RXX_Location_Loc205_write(byteArray, locIndex, instance);break;
    case 204:
    RXX_Location_Loc204_write(byteArray, locIndex, instance);break;
    case 203:
    RXX_Location_Loc203_write(byteArray, locIndex, instance);break;
    case 202:
    RXX_Location_Loc202_write(byteArray, locIndex, instance);break;
    case 201:
    RXX_Location_Loc201_write(byteArray, locIndex, instance);break;
    case 200:
    RXX_Location_Loc200_write(byteArray, locIndex, instance);break;
    case 199:
    RXX_Location_Loc199_write(byteArray, locIndex, instance);break;
    case 198:
    RXX_Location_Loc198_write(byteArray, locIndex, instance);break;
    case 197:
    RXX_Location_Loc197_write(byteArray, locIndex, instance);break;
    case 196:
    RXX_Location_Loc196_write(byteArray, locIndex, instance);break;
    case 195:
    RXX_Location_Loc195_write(byteArray, locIndex, instance);break;
    case 194:
    RXX_Location_Loc194_write(byteArray, locIndex, instance);break;
    case 193:
    RXX_Location_Loc193_write(byteArray, locIndex, instance);break;
    case 192:
    RXX_Location_Loc192_write(byteArray, locIndex, instance);break;
    case 191:
    RXX_Location_Loc191_write(byteArray, locIndex, instance);break;
    case 190:
    RXX_Location_Loc190_write(byteArray, locIndex, instance);break;
    case 189:
    RXX_Location_Loc189_write(byteArray, locIndex, instance);break;
    case 188:
    RXX_Location_Loc188_write(byteArray, locIndex, instance);break;
    case 187:
    RXX_Location_Loc187_write(byteArray, locIndex, instance);break;
    case 186:
    RXX_Location_Loc186_write(byteArray, locIndex, instance);break;
    case 185:
    RXX_Location_Loc185_write(byteArray, locIndex, instance);break;
    case 184:
    RXX_Location_Loc184_write(byteArray, locIndex, instance);break;
    case 183:
    RXX_Location_Loc183_write(byteArray, locIndex, instance);break;
    case 182:
    RXX_Location_Loc182_write(byteArray, locIndex, instance);break;
    case 181:
    RXX_Location_Loc181_write(byteArray, locIndex, instance);break;
    case 180:
    RXX_Location_Loc180_write(byteArray, locIndex, instance);break;
    case 179:
    RXX_Location_Loc179_write(byteArray, locIndex, instance);break;
    case 178:
    RXX_Location_Loc178_write(byteArray, locIndex, instance);break;
    case 177:
    RXX_Location_Loc177_write(byteArray, locIndex, instance);break;
    case 176:
    RXX_Location_Loc176_write(byteArray, locIndex, instance);break;
    case 175:
    RXX_Location_Loc175_write(byteArray, locIndex, instance);break;
    case 174:
    RXX_Location_Loc174_write(byteArray, locIndex, instance);break;
    case 173:
    RXX_Location_Loc173_write(byteArray, locIndex, instance);break;
    case 172:
    RXX_Location_Loc172_write(byteArray, locIndex, instance);break;
    case 171:
    RXX_Location_Loc171_write(byteArray, locIndex, instance);break;
    case 170:
    RXX_Location_Loc170_write(byteArray, locIndex, instance);break;
    case 169:
    RXX_Location_Loc169_write(byteArray, locIndex, instance);break;
    case 168:
    RXX_Location_Loc168_write(byteArray, locIndex, instance);break;
    case 167:
    RXX_Location_Loc167_write(byteArray, locIndex, instance);break;
    case 166:
    RXX_Location_Loc166_write(byteArray, locIndex, instance);break;
    case 165:
    RXX_Location_Loc165_write(byteArray, locIndex, instance);break;
    case 164:
    RXX_Location_Loc164_write(byteArray, locIndex, instance);break;
    case 163:
    RXX_Location_Loc163_write(byteArray, locIndex, instance);break;
    case 162:
    RXX_Location_Loc162_write(byteArray, locIndex, instance);break;
    case 161:
    RXX_Location_Loc161_write(byteArray, locIndex, instance);break;
    case 160:
    RXX_Location_Loc160_write(byteArray, locIndex, instance);break;
    case 159:
    RXX_Location_Loc159_write(byteArray, locIndex, instance);break;
    case 158:
    RXX_Location_Loc158_write(byteArray, locIndex, instance);break;
    case 157:
    RXX_Location_Loc157_write(byteArray, locIndex, instance);break;
    case 156:
    RXX_Location_Loc156_write(byteArray, locIndex, instance);break;
    case 155:
    RXX_Location_Loc155_write(byteArray, locIndex, instance);break;
    case 154:
    RXX_Location_Loc154_write(byteArray, locIndex, instance);break;
    case 153:
    RXX_Location_Loc153_write(byteArray, locIndex, instance);break;
    case 152:
    RXX_Location_Loc152_write(byteArray, locIndex, instance);break;
    case 151:
    RXX_Location_Loc151_write(byteArray, locIndex, instance);break;
    case 150:
    RXX_Location_Loc150_write(byteArray, locIndex, instance);break;
    case 149:
    RXX_Location_Loc149_write(byteArray, locIndex, instance);break;
    case 148:
    RXX_Location_Loc148_write(byteArray, locIndex, instance);break;
    case 147:
    RXX_Location_Loc147_write(byteArray, locIndex, instance);break;
    case 146:
    RXX_Location_Loc146_write(byteArray, locIndex, instance);break;
    case 145:
    RXX_Location_Loc145_write(byteArray, locIndex, instance);break;
    case 144:
    RXX_Location_Loc144_write(byteArray, locIndex, instance);break;
    case 143:
    RXX_Location_Loc143_write(byteArray, locIndex, instance);break;
    case 142:
    RXX_Location_Loc142_write(byteArray, locIndex, instance);break;
    case 141:
    RXX_Location_Loc141_write(byteArray, locIndex, instance);break;
    case 140:
    RXX_Location_Loc140_write(byteArray, locIndex, instance);break;
    case 139:
    RXX_Location_Loc139_write(byteArray, locIndex, instance);break;
    case 138:
    RXX_Location_Loc138_write(byteArray, locIndex, instance);break;
    case 137:
    RXX_Location_Loc137_write(byteArray, locIndex, instance);break;
    case 136:
    RXX_Location_Loc136_write(byteArray, locIndex, instance);break;
    case 135:
    RXX_Location_Loc135_write(byteArray, locIndex, instance);break;
    case 134:
    RXX_Location_Loc134_write(byteArray, locIndex, instance);break;
    case 133:
    RXX_Location_Loc133_write(byteArray, locIndex, instance);break;
    case 132:
    RXX_Location_Loc132_write(byteArray, locIndex, instance);break;
    case 131:
    RXX_Location_Loc131_write(byteArray, locIndex, instance);break;
    case 130:
    RXX_Location_Loc130_write(byteArray, locIndex, instance);break;
    case 129:
    RXX_Location_Loc129_write(byteArray, locIndex, instance);break;
    case 128:
    RXX_Location_Loc128_write(byteArray, locIndex, instance);break;
    case 127:
    RXX_Location_Loc127_write(byteArray, locIndex, instance);break;
    case 126:
    RXX_Location_Loc126_write(byteArray, locIndex, instance);break;
    case 125:
    RXX_Location_Loc125_write(byteArray, locIndex, instance);break;
    case 124:
    RXX_Location_Loc124_write(byteArray, locIndex, instance);break;
    case 123:
    RXX_Location_Loc123_write(byteArray, locIndex, instance);break;
    case 122:
    RXX_Location_Loc122_write(byteArray, locIndex, instance);break;
    case 121:
    RXX_Location_Loc121_write(byteArray, locIndex, instance);break;
    case 120:
    RXX_Location_Loc120_write(byteArray, locIndex, instance);break;
    case 119:
    RXX_Location_Loc119_write(byteArray, locIndex, instance);break;
    case 118:
    RXX_Location_Loc118_write(byteArray, locIndex, instance);break;
    case 117:
    RXX_Location_Loc117_write(byteArray, locIndex, instance);break;
    case 116:
    RXX_Location_Loc116_write(byteArray, locIndex, instance);break;
    case 115:
    RXX_Location_Loc115_write(byteArray, locIndex, instance);break;
    case 114:
    RXX_Location_Loc114_write(byteArray, locIndex, instance);break;
    case 113:
    RXX_Location_Loc113_write(byteArray, locIndex, instance);break;
    case 112:
    RXX_Location_Loc112_write(byteArray, locIndex, instance);break;
    case 111:
    RXX_Location_Loc111_write(byteArray, locIndex, instance);break;
    case 110:
    RXX_Location_Loc110_write(byteArray, locIndex, instance);break;
    case 109:
    RXX_Location_Loc109_write(byteArray, locIndex, instance);break;
    case 108:
    RXX_Location_Loc108_write(byteArray, locIndex, instance);break;
    case 107:
    RXX_Location_Loc107_write(byteArray, locIndex, instance);break;
    case 106:
    RXX_Location_Loc106_write(byteArray, locIndex, instance);break;
    case 105:
    RXX_Location_Loc105_write(byteArray, locIndex, instance);break;
    case 104:
    RXX_Location_Loc104_write(byteArray, locIndex, instance);break;
    case 103:
    RXX_Location_Loc103_write(byteArray, locIndex, instance);break;
    case 102:
    RXX_Location_Loc102_write(byteArray, locIndex, instance);break;
    case 101:
    RXX_Location_Loc101_write(byteArray, locIndex, instance);break;
    case 100:
    RXX_Location_Loc100_write(byteArray, locIndex, instance);break;
    case 99:
    RXX_Location_Loc99_write(byteArray, locIndex, instance);break;
    case 98:
    RXX_Location_Loc98_write(byteArray, locIndex, instance);break;
    case 97:
    RXX_Location_Loc97_write(byteArray, locIndex, instance);break;
    case 96:
    RXX_Location_Loc96_write(byteArray, locIndex, instance);break;
    case 95:
    RXX_Location_Loc95_write(byteArray, locIndex, instance);break;
    case 94:
    RXX_Location_Loc94_write(byteArray, locIndex, instance);break;
    case 93:
    RXX_Location_Loc93_write(byteArray, locIndex, instance);break;
    case 92:
    RXX_Location_Loc92_write(byteArray, locIndex, instance);break;
    case 91:
    RXX_Location_Loc91_write(byteArray, locIndex, instance);break;
    case 90:
    RXX_Location_Loc90_write(byteArray, locIndex, instance);break;
    case 89:
    RXX_Location_Loc89_write(byteArray, locIndex, instance);break;
    case 88:
    RXX_Location_Loc88_write(byteArray, locIndex, instance);break;
    case 87:
    RXX_Location_Loc87_write(byteArray, locIndex, instance);break;
    case 86:
    RXX_Location_Loc86_write(byteArray, locIndex, instance);break;
    case 85:
    RXX_Location_Loc85_write(byteArray, locIndex, instance);break;
    case 84:
    RXX_Location_Loc84_write(byteArray, locIndex, instance);break;
    case 83:
    RXX_Location_Loc83_write(byteArray, locIndex, instance);break;
    case 82:
    RXX_Location_Loc82_write(byteArray, locIndex, instance);break;
    case 81:
    RXX_Location_Loc81_write(byteArray, locIndex, instance);break;
    case 80:
    RXX_Location_Loc80_write(byteArray, locIndex, instance);break;
    case 79:
    RXX_Location_Loc79_write(byteArray, locIndex, instance);break;
    case 78:
    RXX_Location_Loc78_write(byteArray, locIndex, instance);break;
    case 77:
    RXX_Location_Loc77_write(byteArray, locIndex, instance);break;
    case 76:
    RXX_Location_Loc76_write(byteArray, locIndex, instance);break;
    case 75:
    RXX_Location_Loc75_write(byteArray, locIndex, instance);break;
    case 74:
    RXX_Location_Loc74_write(byteArray, locIndex, instance);break;
    case 73:
    RXX_Location_Loc73_write(byteArray, locIndex, instance);break;
    case 72:
    RXX_Location_Loc72_write(byteArray, locIndex, instance);break;
    case 71:
    RXX_Location_Loc71_write(byteArray, locIndex, instance);break;
    case 70:
    RXX_Location_Loc70_write(byteArray, locIndex, instance);break;
    case 69:
    RXX_Location_Loc69_write(byteArray, locIndex, instance);break;
    case 68:
    RXX_Location_Loc68_write(byteArray, locIndex, instance);break;
    case 67:
    RXX_Location_Loc67_write(byteArray, locIndex, instance);break;
    case 66:
    RXX_Location_Loc66_write(byteArray, locIndex, instance);break;
    case 65:
    RXX_Location_Loc65_write(byteArray, locIndex, instance);break;
    case 64:
    RXX_Location_Loc64_write(byteArray, locIndex, instance);break;
    case 63:
    RXX_Location_Loc63_write(byteArray, locIndex, instance);break;
    case 62:
    RXX_Location_Loc62_write(byteArray, locIndex, instance);break;
    case 61:
    RXX_Location_Loc61_write(byteArray, locIndex, instance);break;
    case 60:
    RXX_Location_Loc60_write(byteArray, locIndex, instance);break;
    case 59:
    RXX_Location_Loc59_write(byteArray, locIndex, instance);break;
    case 58:
    RXX_Location_Loc58_write(byteArray, locIndex, instance);break;
    case 57:
    RXX_Location_Loc57_write(byteArray, locIndex, instance);break;
    case 56:
    RXX_Location_Loc56_write(byteArray, locIndex, instance);break;
    case 55:
    RXX_Location_Loc55_write(byteArray, locIndex, instance);break;
    case 54:
    RXX_Location_Loc54_write(byteArray, locIndex, instance);break;
    case 53:
    RXX_Location_Loc53_write(byteArray, locIndex, instance);break;
    case 52:
    RXX_Location_Loc52_write(byteArray, locIndex, instance);break;
    case 51:
    RXX_Location_Loc51_write(byteArray, locIndex, instance);break;
    case 50:
    RXX_Location_Loc50_write(byteArray, locIndex, instance);break;
    case 49:
    RXX_Location_Loc49_write(byteArray, locIndex, instance);break;
    case 48:
    RXX_Location_Loc48_write(byteArray, locIndex, instance);break;
    case 47:
    RXX_Location_Loc47_write(byteArray, locIndex, instance);break;
    case 46:
    RXX_Location_Loc46_write(byteArray, locIndex, instance);break;
    case 45:
    RXX_Location_Loc45_write(byteArray, locIndex, instance);break;
    case 44:
    RXX_Location_Loc44_write(byteArray, locIndex, instance);break;
    case 43:
    RXX_Location_Loc43_write(byteArray, locIndex, instance);break;
    case 42:
    RXX_Location_Loc42_write(byteArray, locIndex, instance);break;
    case 41:
    RXX_Location_Loc41_write(byteArray, locIndex, instance);break;
    case 40:
    RXX_Location_Loc40_write(byteArray, locIndex, instance);break;
    case 39:
    RXX_Location_Loc39_write(byteArray, locIndex, instance);break;
    case 38:
    RXX_Location_Loc38_write(byteArray, locIndex, instance);break;
    case 37:
    RXX_Location_Loc37_write(byteArray, locIndex, instance);break;
    case 36:
    RXX_Location_Loc36_write(byteArray, locIndex, instance);break;
    case 35:
    RXX_Location_Loc35_write(byteArray, locIndex, instance);break;
    case 34:
    RXX_Location_Loc34_write(byteArray, locIndex, instance);break;
    case 33:
    RXX_Location_Loc33_write(byteArray, locIndex, instance);break;
    case 32:
    RXX_Location_Loc32_write(byteArray, locIndex, instance);break;
    case 31:
    RXX_Location_Loc31_write(byteArray, locIndex, instance);break;
    case 30:
    RXX_Location_Loc30_write(byteArray, locIndex, instance);break;
    case 29:
    RXX_Location_Loc29_write(byteArray, locIndex, instance);break;
    case 28:
    RXX_Location_Loc28_write(byteArray, locIndex, instance);break;
    case 27:
    RXX_Location_Loc27_write(byteArray, locIndex, instance);break;
    case 26:
    RXX_Location_Loc26_write(byteArray, locIndex, instance);break;
    case 25:
    RXX_Location_Loc25_write(byteArray, locIndex, instance);break;
    case 24:
    RXX_Location_Loc24_write(byteArray, locIndex, instance);break;
    case 23:
    RXX_Location_Loc23_write(byteArray, locIndex, instance);break;
    case 22:
    RXX_Location_Loc22_write(byteArray, locIndex, instance);break;
    case 21:
    RXX_Location_Loc21_write(byteArray, locIndex, instance);break;
    case 20:
    RXX_Location_Loc20_write(byteArray, locIndex, instance);break;
    case 19:
    RXX_Location_Loc19_write(byteArray, locIndex, instance);break;
    case 18:
    RXX_Location_Loc18_write(byteArray, locIndex, instance);break;
    case 17:
    RXX_Location_Loc17_write(byteArray, locIndex, instance);break;
    case 16:
    RXX_Location_Loc16_write(byteArray, locIndex, instance);break;
    case 15:
    RXX_Location_Loc15_write(byteArray, locIndex, instance);break;
    case 14:
    RXX_Location_Loc14_write(byteArray, locIndex, instance);break;
    case 13:
    RXX_Location_Loc13_write(byteArray, locIndex, instance);break;
    case 12:
    RXX_Location_Loc12_write(byteArray, locIndex, instance);break;
    case 11:
    RXX_Location_Loc11_write(byteArray, locIndex, instance);break;
    case 10:
    RXX_Location_Loc10_write(byteArray, locIndex, instance);break;
    case 9:
    RXX_Location_Loc9_write(byteArray, locIndex, instance);break;
    case 8:
    RXX_Location_Loc8_write(byteArray, locIndex, instance);break;
    case 7:
    RXX_Location_Loc7_write(byteArray, locIndex, instance);break;
    case 6:
    RXX_Location_Loc6_write(byteArray, locIndex, instance);break;
    case 5:
    RXX_Location_Loc5_write(byteArray, locIndex, instance);break;
    case 4:
    RXX_Location_Loc4_write(byteArray, locIndex, instance);break;
    case 3:
    RXX_Location_Loc3_write(byteArray, locIndex, instance);break;
    case 2:
    RXX_Location_Loc2_write(byteArray, locIndex, instance);break;
    case 1:
    RXX_Location_Loc1_write(byteArray, locIndex, instance);break;
    }
}

#endif
// EOF